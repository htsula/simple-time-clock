# Neon Postgres connection string (injected automatically by the Vercel + Neon
# integration; locally, run `vercel env pull .env.local`)
DATABASE_URL="postgres://user:password@host/dbname?sslmode=require"
